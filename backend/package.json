{
  "name": "buildshare-backend",
  "version": "1.0.0",
  "description": "Backend for BuildShare Android distribution platform",
  "scripts": {
    "server": "uv run uvicorn app.main:app --reload",
    "lint": "uv run ruff check . && uv run mypy .",
    "format": "uv run ruff format .",
    "test": "uv run pytest",
    "celery": "uv run celery -A app.tasks.celery_app worker -l info"
  },
  "author": "Antigravity",
  "license": "MIT"
}
